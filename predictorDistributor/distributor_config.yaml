base_url_template: "http://{pred_ip}:{pred_port}"
predictor_pool:
  - id: "worker_1"
    pred_ip: "[host_ip1]" # Replace with worker/ Predictor IP
    pred_port: "8000"
  - id: "worker_2"
    pred_ip: "[host_ip2]" # Replace with worker/ Predictor IP
    pred_port: "8001"
# Add more as necessary